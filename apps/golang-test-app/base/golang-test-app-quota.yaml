---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: golang-test-app-quota
  namespace: golang-test-app
  labels:
    kubernetes.io/metadata.name: golang-test-app
    backstage.io/kubernetes-id: golang-test-app
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
