---
apiVersion: v1
kind: Service
metadata:
  name: test-app-svc
  namespace: test-app
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test-app.diegoluisi.eti.br
  labels:
    app: test-app
    version: 0.0.1
    backstage.io/kubernetes-id: test-app
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: test-app