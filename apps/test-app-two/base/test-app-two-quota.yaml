---
kind: ResourceQuota
apiVersion: v1
metadata:
  name: test-app-two-quota
  namespace: test-app-two
  labels:
    kubernetes.io/metadata.name: test-app-two
    backstage.io/kubernetes-id: test-app-two
    istio-injection: enabled
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
